#include <bits/stdc++.h>
using namespace std;
int main()
{
    // pair<int, int> p = {1, 2};
    // pair<int, pair<int, int>> p = {1, {3, 2}};
    // cout << p.first << "\n";
    // cout << p.second.first << endl;
    // cout << p.second.second;

    // vector<int> v;
    // v.push_back(1);
    // v.push_back(3);
    // v.push_back(14);
    // v.emplace_back(90);

    // for (auto it : v)
    // {
    //     cout << it << endl;
    // }
    // vector<int> v(5, 100);

    // for (auto it : v)
    // {
    //     cout << it << "  ";
    // }

    
    return 0;
}